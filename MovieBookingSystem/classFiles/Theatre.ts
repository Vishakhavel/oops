import ShowTime from "./ShowTime";
import ShpowTime from "./ShowTime";

const getTimeFromString = (time: string) => {};

export default class Theatre {
  // single theatre system.
  //   a collection of showtimes.
  private availableSeats = [];
  constructor() {
    //  no argument for this class.
  }
}
